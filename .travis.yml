language: R
sudo: required
cache: packages
warnings_are_errors: false

before_install:
  - sudo apt-get install --yes udunits-bin libudunits2-dev

r:
  - release
  - devel

r_packages:
  - devtools
  - covr

